version: '3.8'

services:
  simulator:
    build: .
    ports:
      - "5000:5000"
    volumes:
      - ./scenarios:/app/scenarios
      - ./configs:/app/configs
    environment:
      - FLASK_ENV=production
      - FLASK_APP=src/webapp.py
    restart: unless-stopped
